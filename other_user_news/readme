# =========================================================================== #
# NG CMS // ѕлагины // ƒругие новости от пользовател€                         #
# =========================================================================== #

ѕлагин позвол€ет на сайте вывести другие новости от пользователей в полной новости.

ƒл€ работы плагина используетс€ единый TWIG шаблон (по умолчанию other_user_news.tpl).
--------------------------------------------------------------------

ѕоддерживаемые переменные:
  . {{tpl_url}}     - путь к текущему активному шаблону сайта
  . {{author}}      - им€ пользовател€, добавившего новость
  . {{author_link}} - ссылка на пользовател€, добавившего новость
  
* ÷икл {% for entry in entries %}{% endfor %} - шаблон содержимого (строки) информационного блока
  ѕоддерживаемые переменные:
  . {{entry.title}}         - заголовок новости
  . {{entry.news_url}}      - URL ссылка новости
  . {{entry.short_news}}    - коротка€ новость
  . {{entry.postdate}}      - дата добавлени€ новости
  “акже доступны любые другие переменные, из таблицы news, например:
  . {{entry.views}}   - содержимое пол€ views
  
================================================================================
¬арианты использовани€ плагина в шаблонах
================================================================================

ѕример вызова:
{{ callPlugin('other_user_news.show', {'number' : 2, 'mode' : 'dt'}) }}
{{ callPlugin('other_user_news.show', {'number' : 10, 'mode' : 'view', 'template': 'other_user_news', 'cacheExpire': 60 }) }}

ƒоступные переменные (передаютс€ в качестве массива параметров после имени 'other_user_news.show':
* number        - кол-во отображаемых новостей
* mode          - режим сортировки новостей (view, com, dt, rnd)
* template      - им€ шаблона
* cacheExpire   - срок жизни кеша (по умолчанию = 0)
__________________________________________
  ѕоддерживаемые функции                 
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
+ √енерацию блока при помощи TWIG функции callPlugin()
+  еширование
+ Ўаблоны плагина внутри шаблона сайта
