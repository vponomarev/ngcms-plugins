Парсер ответов сервера.

Класс, реализующий общую универсальную обработку полученных XML-ответов серера -
преобразование ответа в массив.

Функции класса:

- _tagOpen($parser, $tag, $attribs)
	# Внутренняя функция, отвечающая за парсинг XML-структуры

- _tagClose($parser, $tag)
	# Внутренняя функция, отвечающая за парсинг XML-структуры

- _tagData($parser, $cdata)
	# Внутренняя функция, отвечающая за парсинг XML-структуры

_change_encoding($data, $encoding)
	# Внутренняя функция, приводящая структуру к требуемой кодировке

- Parse($data, $encoding = "UTF-8")
	# Функция, преобразующая XML в универсальный структурированный массив
	# Параметры:
	# - XML-структура
	# - Кодировка, используемая на сайте

- Reindex($data, $skip_attr = false)
	# Функция, преобразующая универсальный структурированный массив в компактный массив
	# Внимание! Не рекомендуется использовать, если данные содержат списки из однотипных элементов
	# Параметры:
	# - XML-структура
	# - инструкция, отбрасывающая атрибуты XML-нод. Если они не используются, позволяет создать максимально компактную структуру


